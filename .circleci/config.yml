version: 2

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: npm install
  test:
    machine: true
    steps: 
      - checkout
      - run: npm install
      - run: npm test && npm lint

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
